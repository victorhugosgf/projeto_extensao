// Seleciona o botão
const button = document.getElementById('generateScheduleButton');

// Adiciona o evento de clique para redirecionar
button.addEventListener('click', function() {
    // Redireciona para a página 'form.html'
    window.location.href = 'paginas/form.html';
});
